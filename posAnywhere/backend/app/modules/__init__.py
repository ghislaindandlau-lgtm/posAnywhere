# Marks "modules" as a package. Each file here represents one logical service
# from the container diagram (Order, Dispatch, Driver, Settlement, Tracking),
# co-located in a single deployable as a modular monolith.
